services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: prod
    expose:
      - "8090"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - submissions:/submissions
    networks:
      - internal_net
      - coolify
    depends_on:
      - postgres
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: prod
    expose:
      - "3000"
    networks:
      - internal_net
      - coolify
    restart: unless-stopped

  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-dev}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-dev}
      POSTGRES_DB: ${POSTGRES_DB:-testy}
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - internal_net
    restart: unless-stopped

networks:
  internal_net:
    driver: bridge
  coolify:
    external: true

volumes:
  pgdata:
  submissions:
    name: testy_submissions
